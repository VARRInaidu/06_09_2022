#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int fib(int);
int main()
{
int k;
scanf("%d",&k);
int i;
for(i=0;i<k;i++)
{
 printf("%d\t",fib(i));
}
 return 0;
}
int fib(int n)
{
 if(n==0 || n==1)
 return (n);
else
 return(fib(n-1)+fib(n-2));
 }
